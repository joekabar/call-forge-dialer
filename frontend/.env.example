# frontend/.env.example — copy to .env, never commit .env
#
# LOCAL: leave VITE_API_URL blank — vite.config.js proxy handles /api → localhost:8000
# PRODUCTION: set to your deployed backend URL
VITE_API_URL=
VITE_BING_MAPS_KEY=your-bing-maps-embed-key
