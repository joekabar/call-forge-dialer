# backend/requirements.txt
# ─────────────────────────
# Core
fastapi>=0.109.0
uvicorn>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0
email-validator>=2.0.0
# Database
supabase>=2.3.0

# Auth
python-jose[cryptography]>=3.3.0

# Scheduling
APScheduler>=3.10.0

# Data import
openpyxl>=3.1.0
python-multipart>=0.0.6

# Phone number handling
phonenumbers>=8.13.0

# ── Telephony (provider-agnostic) ──────────────────────────
# Twilio (legacy / optional)
twilio>=9.0.0

# HTTP client — Telnyx REST API + VoIPTiger CDR API
httpx>=0.27.0

# Credential encryption (Fernet symmetric encryption)
cryptography>=42.0.0
