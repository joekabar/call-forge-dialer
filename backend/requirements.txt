# backend/requirements.txt
# ─────────────────────────
# Core
fastapi>=0.109.0
uvicorn>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0
email-validator>=2.0.0
# Database
supabase>=2.3.0

# Auth
python-jose[cryptography]>=3.3.0

# Scheduling
APScheduler>=3.10.0

# Data import
openpyxl>=3.1.0
python-multipart>=0.0.6

# Phone number handling
phonenumbers>=8.13.0

# ── Telephony (provider-agnostic) ──────────────────────────
# Twilio (current default provider)
twilio>=9.0.0

# Credential encryption (Fernet symmetric encryption)
cryptography>=42.0.0

# ── v2 additions (uncomment when deploying v2) ─────────────
# httpx>=0.27.0          # Google Solar API calls
# google-cloud-solar     # Google Solar API client
